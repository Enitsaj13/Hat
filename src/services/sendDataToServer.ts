import { ToSendDatus } from "@stores/toSendDatus";

export async function sendDataToServer(data: ToSendDatus) {
  // TODO, create the actual implementation when both send feedback and send observation endpoints are done
  console.log("toSendData", data);
}
